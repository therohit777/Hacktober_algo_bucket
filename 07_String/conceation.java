public class conceation {
    public static void main(String[] args) {
        String firstName = "Tony";
        String lastName = "Stark";
        String fullName = firstName + " " + lastName; // Conceatiton
        System.out.println(fullName);
        System.out.println(fullName.length()); // find the length of the String
    }    
}
